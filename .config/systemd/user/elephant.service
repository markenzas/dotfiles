[Unit]
Description=Elephant backend for Walker
After=graphical-session.target

[Service]
ExecStart=/usr/bin/elephant --config ~/.config/elephant/config.toml
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
